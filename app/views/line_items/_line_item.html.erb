<% if line_item == @current_item  %>
	<tr id="current_item">
	<%else%>
	<tr>
<%end%>

<tr>
		
	<td>  <%=line_item.quantity %> &times; </td>
	<td>  <%=line_item.product.title %> </td>
	<!--<td class= "item_price"> <%=number_to_currency(line_item.product.price) %> </td>-->
	<!--<td class= "item_price"> <%=number_to_currency(line_item.total_price) %> </td> -->
	<td class = "item_price"> <%= number_to_currency(line_item.total_price, :unit => " грн.", :format => "%n%u") %></td>
	<!-- <td> <%=button_to '-', line_item, method: :delete  %> </td> -->
	<%=button_to '-', decrement_line_item_path(line_item), method: :put, remote: true %>
	

</tr>